╔══════════════════════════════════════════════════════════════════════════╗
║  ROOT SECRETS — OFFLINE RECOVERY CARD                                       ║
║  Fill in by hand. Print it. Then DELETE this digital copy.                  ║
║  These are the keys that unlock everything else. They must live OFF every    ║
║  device and outside 1Password — because they're what you use when those      ║
║  are gone. Keep 2 copies in 2 places (e.g. home safe + bank box).           ║
╚══════════════════════════════════════════════════════════════════════════╝

  Filled in on (date):  ____________________

  1) restic backup passphrase  (decrypts the whole cloud backup)
     ......................................................................
     rclone remote / cloud account used:  ________________________________
     account recovery email:              ________________________________

  2) age recovery PRIVATE KEY  (decrypts your 1Password vault exports)
     AGE-SECRET-KEY-......................................................
     (shown once by install-vault-export.sh)

  3) 1Password account  (your day-to-day vault — the "Emergency Kit")
     Sign-in address / email:  ___________________________________________
     Secret Key  (starts A3-...):  _______________________________________
     Account password:  __________________________________________________

  4) FileVault recovery key  (unlocks the Mac's disk)
     ......................................................................
     (System Settings ▸ Privacy & Security ▸ FileVault, if not escrowed to Apple)

  5) Apple Account  (device + iCloud recovery)
     Email:  _____________________________________________________________
     Note: password NOT written here if you can recall it; if written, this
     card must be treated as top-secret.

  ── How to use this card (bare-metal recovery order) ──────────────────────
   a. New Mac → run setup.sh (rebuilds tools) → install-claude-backup.sh
      (uses secret #1) → restic restore.
   b. Move files home; fix ~/.ssh perms; brew bundle from the manifest.
   c. If 1Password itself is unavailable: decrypt the newest vault export with
      secret #2:   age -d -i privkey.txt 1password-YYYY....json.age
      ...then import the .1pux into KeePassXC or Bitwarden.
   d. Full details: CLAUDE-BACKUP.md and START-HERE.md.

  ── What must NEVER live together with this card's secrets ────────────────
   • Secret #1 must not be ONLY in the restic backup (it decrypts that backup).
   • Secrets #2/#3 must not be ONLY in 1Password (they rescue 1Password).
   • That's the whole point: this card is the copy that breaks every loop.
